# Campaign Questions 

## List Questions and Options

> Request

```shell
curl --location --request POST 'http://www.rapidoreach.com/api/v1/campaigns/getProfilingQuestionsForTargeting?enc={{request_hash}}' \
--header 'X-Api-Key: --REPLACE WITH YOUR SECRET KEY--' \
--header 'Content-Type: application/json' \
--data-raw '{
    "CountryLangugageCode": "ENG-US",
    "PartnerCode": "SAMPLY",
    "QuestionId": "5729",
    "UserId": ""
}'
 ```

```ruby
require "uri"
require "net/http"

url = URI("http://www.rapidoreach.com/api/v1/campaigns/getProfilingQuestionsForTargeting?enc={{request_hash}}")

http = Net::HTTP.new(url.host, url.port);
request = Net::HTTP::Post.new(url)
request["X-Api-Key"] = "--REPLACE WITH YOUR SECRET KEY--"
request["Content-Type"] = "application/json"
request.body = "{\n    \"CountryLangugageCode\": \"ENG-US\",\n    \"PartnerCode\": \"SAMPLY\",\n    \"QuestionId\": \"5729\",\n    \"UserId\": \"\"\n}"

response = http.request(request)
puts response.read_body

```

```php
<?php
require_once 'HTTP/Request2.php';
$request = new HTTP_Request2();
$request->setUrl('http://www.rapidoreach.com/api/v1/campaigns/getProfilingQuestionsForTargeting?enc={{request_hash}}');
$request->setMethod(HTTP_Request2::METHOD_POST);
$request->setConfig(array(
  'follow_redirects' => TRUE
));
$request->setHeader(array(
  'X-Api-Key' => '--REPLACE WITH YOUR SECRET KEY--',
  'Content-Type' => 'application/json'
));
$request->setBody('{\n    "CountryLangugageCode": "ENG-US",\n    "PartnerCode": "SAMPLY",\n    "QuestionId": "5729",\n    "UserId": ""\n}');
try {
  $response = $request->send();
  if ($response->getStatus() == 200) {
    echo $response->getBody();
  }
  else {
    echo 'Unexpected HTTP status: ' . $response->getStatus() . ' ' .
    $response->getReasonPhrase();
  }
}
catch(HTTP_Request2_Exception $e) {
  echo 'Error: ' . $e->getMessage();
}
```

```javascript
var myHeaders = new Headers();
myHeaders.append("X-Api-Key", "--REPLACE WITH YOUR SECRET KEY--");
myHeaders.append("Content-Type", "application/json");

var raw = JSON.stringify({"CountryLangugageCode":"ENG-US","PartnerCode":"SAMPLY","QuestionId":"5729","UserId":""});

var requestOptions = {
  method: 'POST',
  headers: myHeaders,
  body: raw,
  redirect: 'follow'
};

fetch("http://www.rapidoreach.com/api/v1/campaigns/getProfilingQuestionsForTargeting?enc={{request_hash}}", requestOptions)
  .then(response => response.text())
  .then(result => console.log(result))
  .catch(error => console.log('error', error));
```

```python
import http.client

conn = http.client.HTTPSConnection("www.rapidoreach.com")
payload = "{\n    \"CountryLangugageCode\": \"ENG-US\",\n    \"PartnerCode\": \"SAMPLY\",\n    \"QuestionId\": \"5729\",\n    \"UserId\": \"\"\n}"
headers = {
  'X-Api-Key': '--REPLACE WITH YOUR SECRET KEY--',
  'Content-Type': 'application/json'
}
conn.request("POST", "/api/v1/campaigns/getProfilingQuestionsForTargeting?enc={{request_hash}}", payload, headers)
res = conn.getresponse()
data = res.read()
print(data.decode("utf-8"))
```

```go
package main

import (
  "fmt"
  "strings"
  "net/http"
  "io/ioutil"
)

func main() {

  url := "http://www.rapidoreach.com/api/v1/campaigns/getProfilingQuestionsForTargeting?enc={{request_hash}}"
  method := "POST"

  payload := strings.NewReader(`{
    "CountryLangugageCode": "ENG-US",
    "PartnerCode": "SAMPLY",
    "QuestionId": "5729",
    "UserId": ""
}`)

  client := &http.Client {
  }
  req, err := http.NewRequest(method, url, payload)

  if err != nil {
    fmt.Println(err)
    return
  }
  req.Header.Add("X-Api-Key", "--REPLACE WITH YOUR SECRET KEY--")
  req.Header.Add("Content-Type", "application/json")

  res, err := client.Do(req)
  if err != nil {
    fmt.Println(err)
    return
  }
  defer res.Body.Close()

  body, err := ioutil.ReadAll(res.Body)
  if err != nil {
    fmt.Println(err)
    return
  }
  fmt.Println(string(body))
}
```


> Response:

```json
{
    "Errors": [],
    "Info": [
        "There are 45 questions found matching with partner id SAMPLY"
    ],
    "Data": [
        {
            "Options": [],
            "_id": "597a2f2f4baa752f9b3a2d66",
            "IsCoreDemographic": true,
            "IsFeasibilityFactor": true,
            "Name": "AGE",
            "QuestionID": "42",
            "QuestionText": "What is your age?",
            "QuestionType": "Numeric - Open-end",
            "SurveyUse": 10330,
            "CountryLangugageCode": "9",
            "DisplayName": "Age"
        },
      ]
}  
```


Retrieves a list of questions scoped to the mapping partner and country.

HTTP Request
POST <code>http://www.rapidoreach.com/api/v1/campaigns/getProfilingQuestionsForTargeting?enc={{request_hash}}</code>

Query Parameters

Parameter |	Required |	Default	| Description
--------- | -------- |  ------- | -----------
enc	| Yes	| None	| Generated request hash